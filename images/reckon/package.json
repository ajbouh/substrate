{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf docs/.observablehq/cache",
    "build": "rm -rf dist && observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "@observablehq/framework": "^1.5.0"
  },
  "devDependencies": {
    "patch-package": "8.0.0"
  },
  "engines": {
    "node": ">=20.6"
  }
}
